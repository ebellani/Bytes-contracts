#lang setup/infotab

(define name "Bytes contracts.")
(define release-notes
  (list '(ul (li "Initial release"))))

(define repositories
  (list "4.x"))
(define blurb
  (list "A list of contracts to bytes related data."))

(define scribblings '(("bytes-contracts.scrbl" ())))
(define primary-file "main.rkt")
(define categories '(datastructures))